{
  "name": "91bot",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "backend",
    "admin"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w frontend\" \"npm run dev -w backend\" \"npm run dev -w admin\"",
    "dev:frontend": "npm run dev -w frontend",
    "dev:backend": "npm run dev -w backend",
    "dev:admin": "npm run dev -w admin",
    "build": "npm run build -w frontend && npm run build -w backend && npm run build -w admin"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
